# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Tooling caches / local env
.tanstack
.env.local
.env.*.local

# src/routeTree.gen.ts is generated but committed so a clean checkout type-checks
# before the first vite run (TanStack Router supports this).

# Test / audit output
/playwright-report
/test-results
/blob-report
/e2e/.cache
/lighthouse/reports

# Markdown working docs — kept out of the repo by request.
# The deliverable README.md and ARCHITECTURE.md must be re-included when written:
#   git add -f README.md ARCHITECTURE.md
*.md
