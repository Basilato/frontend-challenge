# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Tooling caches / local env
.tanstack
.env
.env.local
.env.*.local

# src/routeTree.gen.ts is generated but committed so a clean checkout type-checks
# before the first vite run (TanStack Router supports this).

# Test / audit output
/playwright-report
/test-results
/blob-report
/e2e/.cache

# Visual regression baselines kept out of git by request — they still exist
# locally for `pnpm test:visual` to compare against, just not tracked/pushed.
/e2e/__screenshots__

# lighthouse/reports/ is tracked on purpose — §10/§12 of the challenge require
# shipping the HTML/JSON audit artifacts, not just the runner script.

# Markdown working docs stay out of the repo by request — except the two
# deliverable docs, which are tracked.
*.md
!/README.md
!/ARCHITECTURE.md
